[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: real-runtime tests that shell out to `claude -p` (slow, cost money, non-deterministic). Opt in with `-m integration`.
addopts = -m "not integration"
